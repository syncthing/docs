GET /rest/svc/random/string
====================

Returns a strong random generated string (a-z, A-Z, 0-9) of the specified length. Takes the ``length`` parameter.

.. code-block:: json

	{
	  "random": "FdPaEaZQ56sXEKYNxpgF"
	}
